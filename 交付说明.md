# 代码相似度检测项目交付说明

## 1. 交付内容

本交付包主要包含以下内容：

1. `src/`
   项目核心源码，包含数据处理、代码预处理、特征工程、弱监督标签构造、模型训练、预测、实验分析、论文统计表生成等模块。

2. `scripts/`
   项目辅助脚本，包含论文文档处理、训练图片生成、命令行截图生成、图片插入论文等脚本。

3. `tests/`
   单元测试文件。

4. `dataset/`
   项目实验使用的数据集文件。

5. `outputs_cross_user/`
   跨用户实验结果，包括训练样本、模型文件、相似代码对结果、人工复核样本与统计结果等。

6. `outputs_cross_user_compare/`
   模型对比实验结果。

7. `outputs_ablation/`
   特征消融实验结果。

8. `outputs_question_split/`
   按题目划分的严格泛化实验结果。

9. `paper_tables/`
   论文中可直接使用的统计表。

10. `paper_figures/`
    论文中可直接使用的图片，包括训练过程图、实验结果图和命令行运行示意图。

11. `毕业论文_代码相似度检测_格式修订版_改写.docx`
    当前较新的论文文件。

12. `README.md`
    项目基础说明。

13. `pyproject.toml`
    项目依赖与配置文件。

14. `交付说明.md`
    当前说明文档。

## 2. 项目概述

本项目面向本科毕业设计场景，设计并实现了一套基于弱监督训练建模的代码相似度检测系统。系统以 Java 程序提交数据为输入，通过代码预处理、特征提取、弱监督样本构造和传统机器学习训练，输出代码相似度结果，并生成相应实验统计表和论文图片。

## 3. 运行环境

建议使用本项目本地虚拟环境 `.venv` 运行。

常用命令示例：

```powershell
.venv\Scripts\python.exe -m similarity inspect-data --data-dir dataset
.venv\Scripts\python.exe -m similarity run-pipeline --data-dir dataset --output-dir outputs_cross_user
.venv\Scripts\python.exe -m similarity compare-models --data-dir dataset --output-dir outputs_cross_user_compare --cross-user-only
```

## 4. 论文相关文件说明

论文当前主文件建议使用：

- `毕业论文_代码相似度检测_格式修订版_改写.docx`

如果打开论文后目录、页码或图表编号未立即刷新，建议在 Word 中手动执行：

1. 右键目录，选择“更新域”
2. 右键页码或图表编号，选择“更新域”

## 5. 注意事项

1. 本交付包中未包含 `.venv/`、`.git/`、缓存目录等开发环境文件。
2. 论文中的部分参考文献与封面信息如需最终提交，建议再按学校要求做一次人工核对。
3. 图片和统计表已经单独导出，便于在论文中再次插入或替换。
